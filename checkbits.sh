#!/bin/bash

awk '
{
for (i=1;; i++)
  {
    if ($2>0 && $i>$2)
      break;
    perc = sprintf("%3.2f", (100*($1+$2)+0.01)/$1);
, $1, $2, exp((log(256)*$1)/($1+$2))+0.5) }
 %2d %2d %3.0f\n", (100*($1+$2)+0.01)/$1, $1, $2, exp((log(256)*$1)/($1+$2))+0.5) }
  }
'
